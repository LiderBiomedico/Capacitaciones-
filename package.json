{
  "name": "hospital-capacitaciones",
  "version": "1.0.0",
  "description": "Sistema de Capacitaciones HSLV",
  "main": "index.html",
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod",
    "build": "echo 'Build completed successfully' && exit 0"
  },
  "dependencies": {},
  "devDependencies": {},
  "engines": {
    "node": "18.x"
  }
}